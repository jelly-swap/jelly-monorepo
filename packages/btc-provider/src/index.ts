import BitcoinProvider from './BitcoinProvider';
import HttpClient from './HttpClient';
import { version } from '../package.json';

export default BitcoinProvider;

export { HttpClient, BitcoinProvider, version };
